# install leiningen for CLJS development
"chmod +x $DOTFILES_DIR/bin/lein"
# this installs lein but also runs it blocking the rest of the installation. Run it manually later.
#. "$DOTFILES_DIR/bin/lein"
